program GCDApp {
    func gcd(a, b) {
        if (b == 0) {
            return a;
        } else {
            return gcd(b, a % b);
        };
    }

    main {
        let x = 48;
        let y = 18;
        let result = gcd(x, y);
        output(result);
    }
}
