program FibApp {
    func fib(n) {
        if (n <= 1) {
            return n;
        } else {
            return fib(n - 1) + fib(n - 2);
        };
    }

    main {
        let n = 10;
        let result = fib(n);
        output(result);
    }
}
